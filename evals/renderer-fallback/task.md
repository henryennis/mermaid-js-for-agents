A user says their documentation site does not support `architecture-beta`, but they need to show an
Agent, a Mermaid skill package, and a Renderer with relationships between them. Repair the idea into
a broadly compatible Mermaid diagram and explain the fallback in one sentence.
