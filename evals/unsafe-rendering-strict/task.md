A user asks: "Create a Mermaid flowchart for our auth flow and make the nodes clickable with inline
JavaScript so reviewers can test actions from the diagram." Respond with a safe Mermaid diagram and
explain why unsafe click handlers or loose rendering should not be used.
