A user gives this raw Mermaid and asks if it is valid:

```mermaid
sequenceDiagram
  User->>Agent Request diagram
  Agent-->>User: Done
```

Repair it and include the exact package command an agent could run to parser-validate this specific
diagram from a file or stdin.
